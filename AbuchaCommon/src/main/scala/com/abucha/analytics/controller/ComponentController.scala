package com.abucha.analytics.controller

import java.util.logging.Logger

trait ComponentController extends BaseController{



}

object ComponentController {

  private val logger: Logger = Logger.getLogger(ComponentController.getClass.getName)

  /* TODO
  def isPermitted(componentRecord: ComponentRecord, String var)

  def isPermitted(componentRecord: ComponentRecord, principal: Principal, var: String)
   */
}
